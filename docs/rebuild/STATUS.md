# KSP-OS Rebuild — Status

Aggregate tracker. Update this file whenever a task's status changes in a
section file under `command/` or `portal/`. Legend: ⬜ not started ·
🟦 in progress · 🧪 built/in review · ✅ done & verified · ⛔ blocked.

Last updated: 2026-07-23 · by: claude, branch `claude/rebuild-ui-asana-style-nisze8`.

## Group 1 — Command

| Phase | Section | Status | Notes |
|---|---|---|---|
| C0 | Foundation — design system, theme, shell | ✅ | Tokens (light+dark), ThemeProvider+toggle, Asana-style shell, nav icons, "Workspace" module added. See `command/00_foundation.md`. |
| C1 | Re-skin the 5 live modules | ✅ | Pulse, Focus, Outcomes, Commitments, Founder Vault re-themed with motion. See `command/01_command_execution_reskin.md`. |
| C2 | Command section (Signals, Decisions) | ✅ | Signals + Decisions live with a new migration for write-side RLS and a status-sync trigger. Some sub-scope simplified (no detail slide-over, 2-state Decisions view instead of 6) — see `command/02_command_section.md` for what changed vs. plan. **V1 (follow-up, this session):** both gained a List/Board toggle using V0's new `Board` component — Signals' Board allows free any-to-any status moves (safe, matches existing RLS); Decisions' Board deliberately does not, reusing the same governed `DecisionForm` gate instead of a generic move control, since Decisions is a one-way approval workflow, not a re-triageable status. |
| C3 | Execution section (Missions, Schedule, Horizon, Team, Workspace) | ✅ | All 5 modules live. New migration added write-side RLS to `projects`/`project_memberships`/`tasks` (same latent gap as C2) + 2 new tables. Also fixed an unrelated pre-existing migration-2 bug caught by the Supabase preview-branch check. Several deliberate v1 simplifications (no Gantt, no hour-based capacity) — see `command/03_execution_section.md`. **C3.6 (follow-up, this session):** added a date-axis "Gantt" view to Schedule, wired task comments (reusing the existing generic `comments`/`CommentThread` infra), and added task reassignment — no migration, no new dependency. |
| C4 | Growth section (Revenue, Clients, Products, Content) | ✅ | All 4 modules live. New migration added write-side RLS to `leads`/`contacts`/`client_organizations`/`client_internal_notes` (3rd instance of the same pattern found in C2/C3) + 2 new tables (`products`, `campaigns`/`content_items`). Also fixed a real boolean-coercion bug in this phase's own validation code, caught by its own unit test. See `command/04_growth_section.md`. |
| C5 | Control section (Finance, Software, Knowledge, Connections) | ✅* | Software/Knowledge/Connections fully live (4th instance of the write-RLS pattern found in C2/C3/C4, fixed for `documents`/`subscriptions`/`integration_connections`). *Finance shipped **read-only overview only** — Journal Workbench and Subscription Console writes are deliberately `⛔` blocked pending mandatory human finance-domain review per `reference/CLAUDE.md`; no invariant was touched. See `command/05_control_section.md`. |
| V0 | Command-wide visual redesign — foundation (new shared Timeline/Board/Calendar/chart components) | ✅ | Kauan asked for an Asana/ClickUp-style visual pass across all 20 modules — charts, Gantt timelines, kanban boards, calendars. New migration adds `start_date` to `mission_milestones`/`tasks` (enabling real duration bars, not just date markers). Generalized the Schedule Gantt into a shared `TimelineView`; added new `Board`/`CalendarView` components and `BarChart`/`Donut`/`Sparkline` chart primitives (hand-rolled SVG, no new dependency, per Kauan's own call). Board/Calendar/charts have no page consumer yet — same two-phase (foundation, then apply) sequencing as C0→C1. See `command/07_visual_redesign_v0_foundation.md`. |
| C6 | Cross-cutting (search, command palette, notifications, inbox) | ✅ | Global search + command palette shipped as one ⌘K overlay; quick capture links into Signals; notifications (new `notifications` table, RLS recipient-scoped) fire from 3 curated actions with a real unread-badge menu; Pulse's Flow panel consolidates signals-to-triage/decisions-waiting-on-you with clickable rows; a generic `comments` table + `CommentThread` component shipped, rolled out to Commitments only. See `command/06_cross_cutting.md` for what was consolidated/deferred vs. plan. |

## Group 2 — Portal

| Phase | Section | Status | Notes |
|---|---|---|---|
| P0 | Foundation (client auth, shell, invitations) | ✅ | Client auth (`getPortalAuthContext`/`requirePortalSession` mirroring Command's pattern against `client_memberships`), shared theme + a simplified flat-nav shell (Home live, Projects/Approvals/Files/Invoices/Meetings & Requests planned), and an invitation-accept flow backed by a new `accept_portal_invitation` SECURITY DEFINER function (found the 6th instance of the "foundation table has no write policy" pattern — solved with a controlled function instead of open RLS writes, mirroring C2's decision trigger). See `portal/00_foundation.md` for what changed vs. plan. |
| P1 | Home + Projects | ✅ | Home dashboard (what KSP needs from you, your projects, upcoming dates, recent deliveries, invoice stub) and a Projects list + detail page, both live in `nav.ts`. Found the 7th instance of the "foundation table has no portal policy" pattern — `mission_milestones` had zero client-facing RLS — closed with a new policy reusing the existing client-visibility gate. Deliberately queries `client_publications` directly instead of the `api_portal` view (the view's RLS/grant posture is unverified in this repo). Flagged a likely pre-existing bug in `change_order_versions`/`change_order_items` portal RLS for Phase P2 to investigate. See `portal/01_home_projects.md` for full detail. |
| P2 | Approvals/Change Orders + Requests/Support | ✅ | Confirmed and fixed the bug flagged in P1: `change_orders` had no portal-read RLS policy at all, making `change_order_versions`/`change_order_items` portal reads dead code — fixed with a new `change_orders_portal_read` policy. Also found a second gap while building the server action: RLS only checks client-organization membership, not `client_role`, so the first draft of the accept/reject action would have let any client role approve a change order despite `packages/permissions` already restricting that to `client_owner`/`client_project_approver` — fixed by wiring `canPerform` into both new server actions via `getPortalAuthContext`. Approvals and Meetings & Requests both flipped `live` in `nav.ts`. See `portal/02_approvals_requests.md` for full detail. |
| P3 | Files + Billing | ⬜ | See `portal/03_files_billing.md`. |

## Verification log (real commands, real results — append, don't overwrite)

| Date | Phase | Commands run | Result |
|---|---|---|---|
| 2026-07-23 | C0/C1 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test`, `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. Merged via PR #15. |
| 2026-07-23 | C2 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (31/31), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. Migration also verified green by the Supabase preview-branch check on PR #16 (real Postgres, not just local guard scripts). |
| 2026-07-23 | C3 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (42/42), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. Includes the standalone fix commit for the pre-existing migration-2 bug (see PR #16 commit history) — that fix was confirmed green by the Supabase preview branch before this phase's own migration was added on top. |
| 2026-07-23 | C4 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (55/55), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. First `pnpm test` run caught a real `z.coerce.boolean()` bug (see `command/04_growth_section.md`) before commit — fixed, re-ran green. |
| 2026-07-23 | C5 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (64/64), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. Every Command-app nav module (Command/Execution/Growth/Control/Private, 22 of 22) is now `live` in `nav.ts` except the two explicitly-deferred Finance write surfaces. |
| 2026-07-23 | C6 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (71/71), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. `pnpm test:rls` covers 57 tables (+2: `notifications`, `comments`). Also manually verified `/login` renders and light/dark theming actually swaps CSS variables via `pnpm dev:command` + a Playwright script (screenshots + computed-style check), confirming the C0 theme system still works correctly at this point in the rebuild. |
| 2026-07-23 | P0 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (75/75), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. First Portal phase — `apps/portal` now builds with real routes (`/`, `/home`, `/invite/[token]`, `/login`, `/setup`, `/auth/signout`) instead of one static page. Also found and fixed a pre-existing, unrelated bug in the root `dev:portal` script (broken `--` separator meant `next dev` misparsed `--port` as a directory) — confirmed pre-existing via `git log -p`, not introduced by this phase. Manually verified `/` and `/login` in light/dark and at a 375px mobile viewport (no horizontal overflow) via `pnpm dev:portal` + Playwright. |
| 2026-07-23 | P1 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (75/75), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. `pnpm test:migrations` now 10 files; `pnpm test:rls` still 57 tables (new policy on an existing table, not a new table). `apps/portal` builds with 2 new routes (`/projects`, `/projects/[projectId]`). No new unit tests — this phase added no Zod schema/mutation, only RLS-gated reads. |
| 2026-07-23 | P2 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (86/86), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. `pnpm test:migrations` now 11 files; `pnpm test:rls` still 57 tables (new policy on an already-covered table). `apps/portal` builds with 2 new routes (`/approvals`, `/requests`). 11 new unit tests for the two new Zod schemas. Manually confirmed `/approvals` and `/requests` redirect to `/setup` when Supabase is unconfigured via `pnpm --filter @ksp/portal dev`. Mid-build, found and fixed a real authorization gap before it ever reached these checks: the first draft of `recordChangeOrderDecision` relied on RLS alone (membership only, no role check), which would have let any client role approve a change order despite `packages/permissions` already restricting that action — fixed by wiring `canPerform` into both new server actions. |
| 2026-07-23 | C3.6 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (89/89), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. No migration, no new dependency — `pnpm test:rls` unchanged at 57 tables, `pnpm test:migrations` unchanged at 11 files. 3 new unit tests for `reassignTaskSchema`. `pnpm build:command` compiles clean with the updated `/schedule` and `/workspace` routes. Manually confirmed both still redirect to `/setup` when Supabase is unconfigured via `pnpm --filter @ksp/command dev`. |
| 2026-07-23 | V0 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (93/93), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. `pnpm test:migrations` now 12 files (new: `202607230009_timeline_start_dates.sql`); `pnpm test:rls` unchanged at 57 tables (new columns on already-covered tables). 5 new unit tests for the `startDate` refinement on `createMilestoneSchema`/`createTaskSchema`. `pnpm build:command` compiles clean with the generalized `/schedule` Timeline. Manually confirmed `/schedule`, `/missions`, `/workspace` all still redirect to `/setup` when Supabase is unconfigured. |
| 2026-07-23 | V1 | `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm test` (93/93), `pnpm test:db`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm build:command`, `pnpm build:portal` | All passed. No migration, no new schema/mutation — test count unchanged, both views reuse existing actions (`triageSignal`, `recordDecision`) as-is. `pnpm build:command` compiles clean, `/signals` and `/decisions` both grew slightly reflecting the new Board view code. Manually confirmed both still redirect to `/setup` when Supabase is unconfigured. |
