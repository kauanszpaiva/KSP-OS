# Testing Guide

Run `pnpm test`, `pnpm test:rls`, `pnpm test:migrations`, `pnpm security:secrets`, `pnpm typecheck`, `pnpm lint`, and `pnpm build`. Future vertical slices must add unit, integration, RLS, E2E, security, migration, and accessibility tests matching the blueprint acceptance criteria.
