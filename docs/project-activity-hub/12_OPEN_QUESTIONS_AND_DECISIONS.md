# 12 — Open Questions and Decisions

Status: **Complete** (planning-only) · 2026-07-23

Every decision below is written as **Kauan / KSP owner decision required** — nothing in this document has been approved. Where a recommendation is given, it is this planning pass's best judgment, not an authorization to proceed. Deadlines are expressed relative to the phase they gate (`09_IMPLEMENTATION_ROADMAP.md`), since no calendar date has been set for Phase 0 to begin.

## Decision register

| # | Decision | Why it matters | Options | Recommended | Trade-offs | Owner | Deadline | Status |
|---|---|---|---|---|---|---|---|---|
| D1 | Queue architecture: `pgmq` vs. plain polling-table | Determines a new-vs-no-new-dependency call and the retry/concurrency model | (a) `pgmq` now (b) polling fallback now, revisit later | (b) polling fallback, per `05_SYSTEM_ARCHITECTURE.md` and `adr/0004` | (a) more sophisticated retry semantics but a new extension dependency KSP doesn't need yet at Small scale; (b) simpler, zero new dependency, slightly cruder concurrency control (still safe via `for update skip locked`) | Kauan | Before Phase 0 | Open |
| D2 | GitHub integration identity: GitHub App vs. personal PAT | Affects permission scoping, multi-repo scalability, and whose identity the integration runs under | (a) GitHub App, org-wide install (b) PAT under one user's account | (a) GitHub App | (a) requires an org-owner action to install, cleaner scoping and revocation; (b) faster to set up but ties the integration to one person's account and can't be scoped per-repo as cleanly | Kauan | Before Phase 1 | Open |
| D3 | Vercel event delivery: webhooks vs. polling | Determines real-time vs. near-real-time deployment visibility, and whether a plan-tier upgrade is needed | (a) webhooks (requires Pro/Enterprise, per `11_COST_AND_PLAN_REQUIREMENTS.md`) (b) polling fallback | (b) polling fallback until/unless the plan tier is confirmed available | (a) lower latency, real push-based events; (b) works on any plan, adds polling latency and outbound API call volume | Kauan | Before Phase 2 | Open |
| D4 | Supabase audit mechanism depth | Determines whether project-admin-level actions (not just data/auth events) are visible in the Hub | (a) Auth Audit Logs + Database Webhooks + pgaudit only, all tiers (b) add Platform Audit Logs, Team tier required | (a) as the Phase 3 baseline, revisit (b) if platform-level visibility gaps prove to matter in practice | (a) no cost, narrower coverage; (b) full coverage, recurring plan-tier cost | Kauan | Before Phase 3 | Open |
| D5 | Raw-delivery payload retention window | Balances forensic/debugging value against storage cost and data-minimization principle (`06_SECURITY_PRIVACY_AND_TRUST.md`) | Specific day/month figure per provider category, not yet set | A conservative default (e.g., 90 days for raw payloads, indefinite for normalized/redacted events) — exact figure is Kauan's call, not an architectural constant | Longer retention aids debugging and dispute resolution; shorter retention reduces exposure if a leak ever occurs | Kauan | Before Phase 0 | Open |
| D6 | AI session transcript retention: full transcript vs. summary-only | Directly affects client-confidentiality exposure and storage cost — a transcript may contain client-sensitive content pasted into a prompt | (a) store full transcript, internal-only visibility (b) store `sanitized_summary` only, discard raw transcript after a short window | Store both short-term, then drop the raw transcript after a defined window, keeping only `sanitized_summary` long-term (mirrors D5's raw-vs-normalized retention split) | (a) richer debugging/audit value, higher exposure risk if compromised; (b) safer long-term storage profile, less forensic detail available later | Kauan | Before Phase 4 | Open |
| D7 | Secret storage mechanism for provider credentials | No secret-storage pattern beyond standard env vars exists in the repo today (`00_CURRENT_SYSTEM_AUDIT.md` §19) | (a) Vercel environment variables (current repo convention) (b) Supabase Vault (c) a dedicated secrets manager | (a) for Phase 0–3 (consistent with existing repo convention, zero new infra); revisit only if credential volume/rotation frequency outgrows it | (a) simplest, matches existing pattern; (b)/(c) better rotation/audit ergonomics at the cost of new infrastructure | Kauan | Before Phase 0 | Open |
| D8 | Level 3 correlation confidence threshold for UI surfacing | Determines how aggressive vs. conservative the "inferred, never proven" suggestions feel to a user | A specific numeric threshold (e.g., only surface suggestions above X% confidence) not yet set | Start permissive (surface all Level 3 suggestions with their score) and tune down only if real usage shows too much noise — starting restrictive risks hiding a genuinely useful signal before there's evidence it's noisy | Permissive risks noise; restrictive risks under-surfacing real correlations | Kauan (or delegated to whoever builds Phase 3) | Before Phase 3 | Open |
| D9 | Dual-write to `activity_events`/`audit_events` for Hub-originated events | Consistency with the existing KSP-native audit pattern (`00` §11) vs. avoiding duplicate audit surfaces | (a) Hub events also dual-write to the existing audit tables (b) Hub keeps a fully separate ledger | (a), reusing the existing `record()` helper pattern for any Hub action a KSP user takes (e.g., recording a decision, connecting an integration) — but *not* for high-volume external events themselves (a GitHub push doesn't need a KSP audit-log row, only a Hub ledger row) | (a) keeps one consistent audit story for human actions; risks table bloat if applied to every external event too | Kauan | Before Phase 0 | Open |
| D10 | Canonical event taxonomy versioning | The 23-family taxonomy (`03_CORRELATION_AND_PROVENANCE.md`) will need to evolve as providers add event types | No versioning scheme designed yet | Treat the taxonomy as an additive, backward-compatible enum (new families/subtypes added over time, existing ones never renamed/removed) — formalize as an ADR at Phase 0 rather than deciding ad hoc later | Under-specifying this now risks inconsistent event-family naming once multiple people/agents extend it | Kauan | Before Phase 0 | Open |
| D11 | Pilot project selection for the first vertical slice | `09_IMPLEMENTATION_ROADMAP.md`'s first vertical slice needs one real project + one real GitHub repo + one real Vercel project to prove the pipeline against | Any specific KSP-OS-managed client or internal project | KSP-OS itself (this repository) is the natural pilot — it already has real GitHub/Vercel/Supabase activity and no client-confidentiality exposure from using it as the test subject | Using KSP-OS itself is low-risk but doesn't validate a client-facing repository's permission/branch conventions; a client project validates realism but adds confidentiality handling to the pilot | Kauan | Before Phase 0 kickoff | Open |
| D12 | Anthropic organization admin-key acquisition | Gates org-wide Claude API/Claude Code cost rollups (`11_COST_AND_PLAN_REQUIREMENTS.md`) | Acquire now vs. defer until per-session figures prove insufficient | Defer until Phase 4 planning, since per-session cost figures cover the first vertical slice and Phase 0–3 needs | Acquiring early enables rollups sooner; deferring avoids provisioning a credential before it's needed | Kauan | Before Phase 4 | Open |
| D13 | OpenAI organization admin-key acquisition | Same as D12, for Codex/OpenAI-side cost rollups | Same shape as D12 | Defer until Phase 4b planning | Same as D12 | Kauan | Before Phase 4b | Open |
| D14 | Vercel plan-tier upgrade for webhook eligibility | Gates D3's webhook option | Upgrade now vs. stay on polling fallback | Stay on polling fallback until a concrete latency/reliability complaint justifies the upgrade cost | Upgrading sooner improves latency; staying avoids an unproven recurring cost | Kauan | Before Phase 2 | Open |
| D15 | Supabase Team-tier upgrade for Platform Audit Logs | Gates D4's fuller-coverage option | Same shape as D14 | Same reasoning as D14 — defer until a concrete gap is felt | Same as D14 | Kauan | Before Phase 3 | Open |
| D16 | Notification/alert thresholds and batching policy | `07_UX_INFORMATION_ARCHITECTURE.md` deliberately left exact thresholds to implementation time | Specific numeric thresholds not yet set (e.g., what counts as an "excessive webhook failure rate") | Set conservative initial thresholds at Phase 0/1 implementation time, tune based on real signal-to-noise experience — do not hardcode a guessed number into this planning document | Under-alerting misses real problems; over-alerting recreates the "notify on everything" anti-pattern this plan explicitly rejects | Whoever implements Phase 1 (with Kauan's sign-off on the initial thresholds) | Before Phase 1 | Open |
| D17 | On-call / SLA policy for the runbooks in `10_OPERATIONS_AND_RUNBOOKS.md` | Runbooks describe *what* to do, not *who* is paged or *how fast* | No policy exists yet — this is an organizational decision, not an architectural one | Not architecturally recommended here — genuinely Kauan's call as the business owner | N/A | Kauan | Before Phase 0 (or accepted as "best-effort, no formal SLA" if that's the honest current state of KSP's ops) | Open |
| D18 | Credential rotation cadence | `10`'s R12 runbook describes *how* to rotate but not *how often* | No cadence set | A reasonable default (e.g., annual, or on role/staff change) pending Kauan's actual policy preference | Frequent rotation is safer but higher operational overhead for a small team | Kauan | Before Phase 0 | Open |
| D19 | Phase 7 (Controlled Action Center) scope and approval | `09`'s roadmap explicitly declined to design this phase, given its blast-radius (the Hub taking real actions in external systems, not just observing) | Design now vs. defer to a dedicated future planning pass | Defer — requires its own explicit planning-and-approval cycle, not a sub-section of this one | Designing now risks under-scoping a genuinely high-stakes capability; deferring means Phases 0–6 ship real value first without waiting on it | Kauan | Before any Phase 7 work begins | Open |
| D20 | Client-safe progress reports | `07`'s report-types list flags this as "a distinct, separately-scoped future concept," not built in this plan | Build as part of this plan vs. treat as fully separate | Treat as fully separate — this plan's reports are internal-only; a client-facing variant needs its own scope/privacy pass (what an external client should never see from the Activity ledger is a materially different question than what a KSP staff member should see) | Folding it in now risks under-scoping client-data exposure; keeping it separate delays a potentially valuable client-facing feature | Kauan | Whenever a future Portal-facing activity feature is considered | Open |
| D21 | New "Mission detail" page and its sub-navigation | `07_UX_INFORMATION_ARCHITECTURE.md` proposes a page that doesn't exist today (Missions is currently list-only) — this is itself a real information-architecture change beyond just adding Hub screens | Build the Mission detail page as part of this plan vs. treat it as a prerequisite Command-side change to be planned/approved on its own | Confirm as an accepted prerequisite before Phase 1 begins, since nearly every Hub screen in `07` assumes it exists | Building it as part of Hub work keeps the two efforts together; splitting it out delays Hub UI work until the Mission detail page lands separately | Kauan | Before Phase 1 | Open |
| D22 | New top-level "Ops Center" nav item | The one genuinely new *top-level* nav addition this plan proposes (`07`) | Add it vs. fold cross-project visibility into an existing nav item (e.g., Pulse) | Add as its own item, tagged `planned` until built, per the existing `NavItem` convention — folding it into Pulse would blur Pulse's existing company-outcomes focus with dev/deployment-health focus | Separate item keeps concerns distinct; folding in avoids nav sprawl | Kauan | Before Phase 6 | Open |

## Readiness gate

Per the PDF's requirement, this planning pass must issue exactly one of: `READY_FOR_FIRST_VERTICAL_SLICE`, `CONDITIONALLY_READY`, `NOT_READY`.

### Readiness checklist

| Question | Answer |
|---|---|
| Is the current system fully audited with cited evidence? | Yes — `00_CURRENT_SYSTEM_AUDIT.md`, 28 sections + Evidence Index |
| Are all four integration surfaces (GitHub, Vercel, Supabase, AI agents/MCP) capability-mapped with confidence levels? | Yes — `01_INTEGRATION_CAPABILITY_MATRIX.md` |
| Is the product scope bounded, with explicit non-goals? | Yes — `02_PRODUCT_SCOPE.md` |
| Is the correlation/provenance model fully specified, including what is never auto-promoted to "proven"? | Yes — `03_CORRELATION_AND_PROVENANCE.md` |
| Is a conceptual data model specified with explicit reuse-vs-new-table decisions? | Yes — `04_DATA_MODEL.md` |
| Is the ingestion/processing architecture specified, including failure/retry/dead-letter handling? | Yes — `05_SYSTEM_ARCHITECTURE.md` |
| Is a threat model and retention policy specified? | Yes — `06_SECURITY_PRIVACY_AND_TRUST.md` |
| Is the UX/IA specified against KSP OS's *actual* existing navigation, not an assumed generic one? | Yes — `07_UX_INFORMATION_ARCHITECTURE.md` |
| Is a test/verification plan specified across unit/integration/e2e/security/performance? | Yes — `08_TEST_AND_VERIFICATION_PLAN.md` |
| Is a phased implementation roadmap with a concrete first vertical slice and a numbered backlog specified? | Yes — `09_IMPLEMENTATION_ROADMAP.md` |
| Are operations/runbooks specified for the realistic failure modes? | Yes — `10_OPERATIONS_AND_RUNBOOKS.md` |
| Is a cost/plan-requirements analysis specified? | Yes — `11_COST_AND_PLAN_REQUIREMENTS.md` |
| Are all architecturally-significant choices recorded as ADRs? | Yes — see `adr/` (all `Proposed`, none `Accepted`) |
| Has any code, migration, or dependency actually been added to production? | **No** — by design, per the PDF's own constraint |
| Has any provider integration, webhook, OAuth app, or token actually been created? | **No** — by design |
| Are there decisions only Kauan can make before Phase 0 work can safely start? | **Yes** — D1, D5, D7, D9, D10, D11, D18 are all tagged "Before Phase 0" above and remain open |
| Does the plan honestly disclose its own unknowns rather than asserting false confidence? | Yes — every UNKNOWN from the research agents is preserved as such in `01` rather than resolved by guessing |
| Would starting Phase 0 implementation today, with zero further decisions, violate any of KSP's non-negotiable governance rules? | No architectural violation, but D1/D5/D7/D9/D10/D11/D18 being unresolved means a builder would have to guess at defaults the owner should actually set |

### Verdict: `CONDITIONALLY_READY`

The plan itself is complete, internally consistent, and fully cited. It is **not** `READY_FOR_FIRST_VERTICAL_SLICE` outright only because seven "Before Phase 0" decisions (D1, D5, D7, D9, D10, D11, D18) are genuinely Kauan's to make, not this planning pass's to assume — proceeding without them would mean an implementer silently picking defaults on Kauan's behalf, which is exactly the kind of unapproved business-rule invention `reference/AGENTS.md` prohibits. It is **not** `NOT_READY`, because none of the open items require further research, further architecture work, or resolve any actual planning gap — they are pure owner decisions with a clearly stated recommendation already in hand for each one.

**Path to `READY_FOR_FIRST_VERTICAL_SLICE`**: Kauan reviews the decision register above and either accepts each recommendation as-is or overrides it. Once D1, D5, D7, D9, D10, and D11 are resolved (D18 can reasonably default to "best-effort, revisit later" without blocking Phase 0), Phase 0 of `09_IMPLEMENTATION_ROADMAP.md` can begin.
