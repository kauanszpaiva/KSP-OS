## Purpose

## Spec compliance

- Authoritative spec/source:
- Requirements affected:
- Verdicts (`implemented` / `partial` / `absent` / `contradicted` / `undecidable`):
- Evidence / searches:
- Unresolved gaps and severity:
- Fix belongs in: code / tests / configuration / documentation / external release gate

## Scope

## Migrations

## Security / RLS / approvals impact

## Test evidence

Record the exact commands/checks that actually ran and their exact result. Do not mark skipped checks as passing.

## Known limitations

## Rollback considerations
